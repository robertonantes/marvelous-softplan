import React from "react";
import { Button, TextInput } from "./SearchForm.styles";

const SearchForm = () => {
  return (
    <form>
      <TextInput placeholder="Search" />
      <Button />
    </form>
  );
};

export default SearchForm;
